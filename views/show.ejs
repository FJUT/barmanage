<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/html">
<head>
  <title>后台管理</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
  <link rel="stylesheet" href="/css/bootstrap.css">
  <style>
    body {
      background-color: #000000;
        height:100%;
        overflow: hidden;
    }
    
    .time {
      font-size: 24px;
      width: 100px;
      box-sizing: border-box;
      padding: 10px;
      color: #FFF;
    }

    .bubble {
      display: flex;
      flex-direction: row;
      position: absolute;
      top: 50%;
        left: 50%;
      width: 600px;

      margin-top: -100px;
        margin-left: -300px;
      overflow: hidden;
    }

    .avatar_wrap {
      width: 60px;
      height: 60px;
      border-radius: 30px;
      border: 1px solid #ccc;
      overflow: hidden;
    }

    .avatar {
      width: 60px;
      height: 60px;
    }

    .message_wrap {
      margin-left: 10px;
      display: flex;
      flex-direction: column;
      flex: 1;
      font-family: "Microsoft JhengHei";
      font-weight: bolder;
    }

    .user {
      color: #FFFFFF;
      font-size: 24px;
    }

    .message {
        background-color: #FFFFFF;
        padding: 10px;
        border-radius: 10px;
          font-size: 40px;
          color: #f0703d;
          flex: 1;
        margin-top: 10px;
        max-height: 300px;
        overflow: hidden;
        text-overflow: ellipsis;
    }

    .picture {
        margin-top: 10px;
    }
  </style>
</head>
<body>
<div class="container">
  <div id="app">
    <transition enter-active-class="bounceInDown" leave-active-class="bounceOutDown">
      <div v-if="curr" class="bubble animated tada">
        <div class="avatar_wrap"><img :src="curr.avatar" class="avatar"/></div>
        <div class="message_wrap">
          <div class="user">{{curr.user}}</div>
          <div class="message" v-if="curr.message">{{curr.message}}</div>
          <div class="picture" v-if="curr.image"><img :src="curr.image"/></div>
        </div>
        <div v-if="curr" class="time">{{time}}</div>
      </div>
    </transition>
  </div>
</div>
<script src="<%=url('init.js')%>"></script>
<script src="<%=url('show.bundle.js')%>"></script>
</body>
</html>