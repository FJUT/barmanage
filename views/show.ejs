<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/html">
<head>
  <title>后台管理</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
  <link rel="stylesheet" href="/css/bootstrap.css">
  <style>
    body {
      background-color: #000000;
    }
    
    .time {
      position: relative;
      left: 50%;
      top: 20px;
      -webkit-transform: translate(0, -50%);
      -moz-transform: translate(0, -50%);
      -ms-transform: translate(0, -50%);
      -o-transform: translate(0, -50%);
      transform: translate(0, -50%);
      color: #FFF;
    }

    .bubble {
      display: flex;
      flex-direction: row;
      margin: 0 auto;
      position: absolute;
      top: 50%;
      width: 600px;
      height: 200px;
      margin-top: -100px;
      overflow: hidden;
    }

    .avatar_wrap {
      width: 60px;
      height: 60px;
      border-radius: 30px;
      border: 1px solid #ccc;
      overflow: hidden;
    }

    .avatar {
      width: 60px;
      height: 60px;
    }

    .message_wrap {
      margin-left: 10px;
      display: flex;
      flex-direction: column;
      flex: 1;
      font-family: "Microsoft JhengHei";
      font-weight: bolder;
    }

    .user {
      color: #FFFFFF;
      font-size: 24px;
    }

    .message {
      font-size: 60px;
      color: #f0703d;
    }

    .picture {

    }
  </style>
</head>
<body>
<div class="container">
  <div id="app">
    <h1 v-if="curr" class="time">{{time}}</h1>
    <transition enter-active-class="bounceInDown" leave-active-class="bounceOutDown">
      <div v-if="curr" class="bubble animated">
        <div class="avatar_wrap"><img :src="curr.user" class="avatar"/></div>
        <div class="message_wrap">
          <div class="user">吴亦凡</div>
          <div class="message" v-if="curr.message">{{curr.message}}</div>
          <div class="picture" v-if="curr.image"><img :src="curr.image"/></div>
        </div>
      </div>
    </transition>
  </div>
</div>
<script src="<%=url('init.js')%>"></script>
<script src="<%=url('show.bundle.js')%>"></script>
</body>
</html>